// Shared palette — used by both the control panel and the 3D stage.

/** Hidden-unit accents, indexed by position within the layer. */
export const UNIT_COLORS = ['#c084fc', '#4ade80', '#f472b6'];
/** The output unit / decision boundary. */
export const OUT_COLOR = '#fbbf24';
/** Point colors for class 0 / class 1. */
export const CLASS_COLORS = ['#2fa8ff', '#ff8a3d'];
